/** @type {import('tailwindcss').Config} */

// Египетская палитра «Спокойная» — утверждена фаундером 13.08.2026.
// Золото намеренно НЕ основной цвет: он утомляет глаза при долгой работе,
// поэтому бронза здесь редкий акцент, а текст — тёплый песок.
//
// Экраны красятся отсюда, а не каждый по отдельности: до этого семнадцать
// файлов держали цвета в своих классах, и правка палитры расползалась по
// всем. Теперь смена темы — правка этого файла.
//
// Серая шкала переопределена намеренно: `gray-*` в разметке осталась, но
// это больше не синий графит Tailwind, а тёплый камень. Так старая разметка
// продолжает работать без правки, а цвет приходит из одного места.
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {
      colors: {
        primary: '#a8925f',        // бронза — акцент, кнопки, активный пункт
        'primary-bright': '#c2ac7c',
        secondary: '#4d729f',      // лазурит
        torch: '#d98c52',          // факел — предупреждения
        teal: '#5eb8ac',           // холодный статус «живо»
        dark: '#232019',           // фон карточки
        darker: '#1b1915',         // фон страницы
        // Джарвис вне египетской мифологии — холодный графит.
        // Отдельные токены, чтобы его нельзя было случайно «озолотить».
        jarvis: {
          bg: '#14171a',
          panel: '#1b1f22',
          line: '#43494d',
          dim: '#7c8287',
          bright: '#a3abb0',
        },
        gray: {
          100: '#e0d6bb',
          200: '#cbbfa0',
          300: '#b5a889',
          400: '#948a72',
          500: '#8a8069',
          600: '#6e6553',
          700: '#4a4436',
          800: '#3c372c',
          900: '#2a271e',
        },
      },
      fontFamily: {
        // Заголовки — та же гарнитура, что в утверждённом макете Пантеона
        display: ['"Iowan Old Style"', '"Palatino Linotype"', 'Palatino', 'Georgia', 'serif'],
      },
    },
  },
  plugins: [],
}
