# NEXSYS Backend Dependencies
# Core Framework
fastapi==0.115.12
uvicorn[standard]==0.34.3
pydantic==2.11.5
pydantic-settings==2.7.1

# Vector Store
chromadb==0.6.3

# Security
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.20

# Environment Variables
python-dotenv==1.0.1

# HTTP Client
httpx==0.28.1

# Utilities
networkx==3.5
# Межпроцессный лок для JSON-хранилищ (core/jsonio.py, locked_update) —
# веб-чат и Telegram-бот разные процессы, threading.Lock между ними не
# работает; filelock — маленький, без транзитивных зависимостей, тот же
# класс задачи, что уже решён вручную в core/singleton.py, но там только
# для PID-файлов расписания/бота, не для данных.
filelock==3.16.1

# Hermes (Telegram-слой, планировщик Dream Cadence, Google-клиенты)
python-telegram-bot==22.8
APScheduler==3.11.3
google-auth==2.56.3
google-auth-oauthlib==1.4.0
google-api-python-client==2.198.0

# Компьютерное управление (шаг 3, 19.08.2026) — скриншот, клик, ввод текста
pyautogui==0.9.54
# pyperclip используется напрямую в computer_use.py (вставка текста через
# буфер обмена — typewrite() не умеет кириллицу), не только как
# транзитивная зависимость pyautogui — прописан отдельно, найдено
# код-ревью 19.08.2026, тот же класс проблемы, что раньше был с
# numpy/sentence-transformers
pyperclip==1.11.0

# Команды компьютеру: плеер, громкость, окна, состояние машины
# (25.08.2026, services/media_control.py + services/system_control.py).
#
# winsdk — доступ к WinRT GlobalSystemMediaTransportControls: список
# медиа-сессий по именам приложений и команды в конкретную из них. Без
# него остаются только медиа-клавиши, а они идут в ту сессию, которую
# Windows считает текущей — на машине фаундера это оказался Telegram, и
# «следующий трек» перематывал голосовое сообщение вместо песни.
#
# pycaw — громкость системы уровнем («поставь громкость сорок»), чего
# медиа-клавиши не умеют в принципе. Тянет за собой comtypes.
#
# psutil и PyGetWindow до сих пор стояли транзитивно (psutil — от chromadb,
# PyGetWindow — от pyautogui). Теперь их зовут напрямую, значит прописаны
# явно: та же причина, по которой отдельно прописан pyperclip выше.
winsdk==1.0.0b10
pycaw==20251023
psutil==7.2.2
PyGetWindow==0.0.9

# Локальный синтез речи (24.08.2026). Заменил edge-tts как движок по
# умолчанию после замеров: edge отдавал первый байт звука за 2.4, 3.7 и
# 37.4 секунды в трёх замерах подряд (серверы Microsoft, разброс сетевой),
# piper на этом же процессоре — 0.41-0.66 сек. Сами голоса (~61 МБ) в git
# не хранятся, см. voice_engine/piper_voices/README.md.
piper-tts==1.7.0

# Слово-будильник «Джарвис» в фоне для плавающего виджета (19.08.2026) —
# офлайн-распознавание Vosk + захват микрофона, отдельный процесс
# wakeword/server.py (см. его же docstring за причиной), но в тот же venv,
# что и backend/ — конфликтов версий с FastAPI/Starlette не даёт (в
# отличие от OmniVoice, где torch требует своего изолированного venv).
vosk==0.3.45
sounddevice==0.5.6

# Testing
pytest==8.4.1
pytest-asyncio==0.25.3
pytest-cov==6.0.0

# Code Quality (optional, for development)
# ruff==0.9.6
# black==25.1.0
# mypy==1.15.0
